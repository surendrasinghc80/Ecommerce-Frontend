const path = require("path");

module.exports = {
  config: path.resolve("server", "config/config.mjs"),
  "seeders-path": path.resolve("server", "seeders"),
  "migrations-path": path.resolve("server", "migration"),
  "model-path": path.resolve("server", "model"),
};
